{
  "private": true,
  "workspaces": [
    "modules/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=modules/crypto && npm run build --workspace=modules/json && npm run build --workspace=modules/util && npm run build --workspace=modules/dag && npm run build --workspace=modules/dag_test && npm run build --workspace=modules/dag_sql && npm run build --workspace=modules/replica"
  }
}
